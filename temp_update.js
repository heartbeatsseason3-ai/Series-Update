const fs = require('fs'); const glob = require('fs').readdirSync; const files = glob('f:/WEBSITE/series update').filter(f => f.endsWith('.html')); for (const file of files) { const path = 'f:/WEBSITE/series update/' + file; let content = fs.readFileSync(path, 'utf8'); content = content.replace(/src=\"script\.js(\?v=\d+)?\"/g, 'src=\"script.js?v=' + Date.now() + '\"'); fs.writeFileSync(path, content); } console.log('Updated');
